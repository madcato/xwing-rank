<%= form_for(@tourney, role: "form") do |f| %>
  <% if @tourney.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tourney.errors.count, "error") %> prohibited this tourney from being saved:</h2>

      <ul>
      <% @tourney.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :state %><br>
	<%= select("tourney", "state", collectTournamentStates(), class: "form-control") %>
  </div>
  <div class="form-group">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :playDate %><br>
		<%= date_select :tourney, :playDate %>
  </div>
  <div class="form-group">
    <%= f.label :publicId %><br>
		<%= f.text_field :publicId, class: "form-control" %>
  </div>
		
  <%= f.submit class: "btn btn-default" %>
<% end %>
